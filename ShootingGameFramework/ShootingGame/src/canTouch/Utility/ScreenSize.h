#pragma once

#include <TktkMath/Vector2.h>

// スクリーンサイズX
const float SCREEN_SIZE_X = 1280.0f;

// スクリーンサイズY
const float SCREEN_SIZE_Y = 720.0f;

// スクリーンサイズを指定
const Vector2 SCREEN_SIZE = Vector2(SCREEN_SIZE_X, SCREEN_SIZE_Y);